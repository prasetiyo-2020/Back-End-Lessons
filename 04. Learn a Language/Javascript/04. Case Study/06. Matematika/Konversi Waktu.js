
// Konversi Menit ke Detik
const menitKeDetik = function (menit) {
    return menit * 60;
}

// Konversi Detik ke Menit
const detikKeMenit = (detik) => {
    return detik / 60;
}

// Konversi Jam ke Menit
const jamKeMenit = function (jam) {
    return jam * 60;
}

// Konversi Menit ke Jam
const menitKeJam = (menit) => {
    return menit / 60;
}

// Konversi Hari ke Jam
const hariKeJam = function (hari) {
    return hari * 24;
}

// Konversi Jam ke Hari
const jamKeHari = (jam) => {
    return jam / 24;
}

// Konversi Minggu ke Hari
const mingguKeHari = function (minggu) {
    return minggu * 7;
}

// Konversi Hari ke Minggu
const hariKeMinggu = (hari) => {
    return hari / 7;
}

// Konversi Bulan ke Hari
const bulanKeHari = function (bulan) {
    return bulan * 30;
}

// Konversi Hari ke Bulan
const hariKeBulan = (hari) => {
    return hari / 30;
}

// Konversi Bulan ke Minggu
const bulanKeMinggu = function (bulan) {
    return bulan * 4;
}

// Konveri Minggu ke Bulan
const mingguKeBulan = (minggu) => {
    return minggu / 4;
}

// Konversi Tahun ke Bulan
const tahunKeBulan = function (tahun) {
    return tahun * 12;
}

// Konversi Bulan ke Tahun
const bulanKeTahun = (bulan) => {
    return bulan / 12;
}

// Konversi Tahun ke Minggu
const tahunKeMinggu = function (tahun) {
    return tahun * 48;
}

// Konversi Minggu ke Tahun
const mingguKeTahun = (minggu) => {
    return minggu / 48;
}

// Konversi Windu ke Tahun
const winduKeTahun = function (windu) {
    return windu * 8;
}

// Konversi Tahun Ke Windu
const tahunKeWindu = (tahun) => {
    return tahun / 8;
}

// Konversi Dekade ke Tahun
const dekadeKeTahun = function (dekade) {
    return dekade * 10;
}

// Konversi Tahun ke Dekade
const tahunKeDekade = (tahun) => {
    return tahun / 10;
}

// Konversi Dasawarsa ke Tahun
const dasawarsaKeTahun = function (dasawarsa) {
    return dasawarsa * 10;
}

// Konversi Tahun ke Dasawarsa
const tahunKeDasawarsa = (tahun) => {
    return tahun / 10;
}

// Konversi Abad ke Tahun
const abadKeTahun = function (abad) {
    return abad * 100;
}

// Konversi Tahun ke Abad
const tahunKeAbad = (abad) => {
    return abad / 100;
}

// Konversi Milenium ke Tahun
const mileniumKeTahun = function (milenium) {
    return milenium * 1000;
}

// Konversi Tahun ke Milenium
const tahunKeMilenium = (tahun) => {
    return tahun / 1000;
}
